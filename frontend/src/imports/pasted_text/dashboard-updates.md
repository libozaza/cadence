DASHBOARD UPDATES — paste this as an update to the existing dashboard prompt:
Remove the History item from the sidebar navigation entirely. The sidebar should only have: Dashboard, Find Care, and Settings.
Add a time range toggle bar directly on the dashboard, positioned above all three charts as a single shared control. The toggle has six options: 1D, 1W, 1M, 6M, 1Y, All. Style it as a pill-shaped segmented control in warm gray, with the selected option highlighted in sage green. Selecting a toggle filters all three charts simultaneously.
Add a third chart card alongside the existing Hold time and Flight time charts, labeled "Latency time (inter-key interval)." Same visual treatment — sage green user data line, warm gray shaded reference band for healthy population range, minimal gridlines, x-axis showing the selected time range, y-axis in milliseconds.
Arrange the three charts in a row across the full width of the main panel below the score gauge and feature breakdown cards. Each chart takes equal width.
Increase all sidebar text, icons, and buttons significantly for elderly readability. Nav item labels should be at least 16px, nav icons 22px, and the Export report and Find a neurologist buttons should be taller with at least 16px text. The "All data stored locally" label at the bottom can remain small.

FIND CARE SCREEN — new screen, same app shell:
Keep the identical sidebar and top bar from the dashboard. The main panel shows the Find Care content.
At the top of the main panel, a large friendly heading: "Find a neurologist near you." Below it, a subtitle in muted text: "Share your Cadence report with a movement disorder specialist."
Below the heading, a large search row: a zip code input field (very large, at least 18px text, tall enough for easy tapping) and a "Search" button in sage green, same height as the input. Below the input, a simple radius selector displayed as three large pill buttons side by side: "5 miles", "10 miles", "25 miles" — default selected is 10 miles, highlighted in sage green.
Below the search row, a results list. Show 3 provider result cards stacked vertically. Each card is large with generous padding. Inside each card: provider name in large bold text (at least 18px), specialty below it in muted text ("Movement Disorder Specialist" or "Neurologist"), then distance on the same line as specialty. Below that, the phone number displayed in very large text (at least 20px, sage green color so it reads as tappable) with a phone icon to the left. Below the phone number, a large "Visit website" button — full width of the card, outlined style in sage green. Generous spacing between all elements. No small text anywhere on this screen. Cards have soft rounded corners and a warm border.
At the bottom of the results list, a muted note in slightly larger text than typical disclaimers: "Results shown are for informational purposes. Cadence does not endorse any specific provider."

SETTINGS SCREEN — new screen, same app shell:
Keep the identical sidebar and top bar. Main panel shows Settings content.
Large section heading at top: "Settings." Three distinct sections separated by warm gray dividers:
Section 1 — "Monitoring": A single large row with the label "Background keystroke monitoring" on the left in large text (16px+), a brief muted description below the label ("Cadence monitors your typing quietly in the background"), and a large toggle switch on the right. Toggle is sage green when on.
Section 2 — "Notifications": Section heading followed by two rows. First row: label "Notify me if patterns are elevated" with a large toggle. Second row (only visible if toggle is on): a slider control labeled "Alert threshold" with a muted description below it reading: "You'll be notified if your motor pattern score stays elevated for 3 or more consecutive weeks. This follows the 7-day scoring window used in published research (Tripathi et al., IEEE 2023 / Michael J. Fox Foundation)." The slider goes from 20% to 80% with a default at 50%. Display the current threshold value in large sage green text next to the slider. Below the slider, a plain-language preview of what will happen: "If your score stays above [X]% for 3 weeks in a row, Cadence will suggest speaking with a neurologist." All text on this section at least 15px.
Section 3 — "Privacy & data": Section heading followed by two rows. First row: a "View privacy information" button — full width, outlined, opens the onboarding privacy step (step 2) as a modal overlay. Second row: a "Clear all my data" button — full width, outlined in muted warm red (not alarming, just distinct), with a muted description below it: "This will permanently delete all keystroke data and scores stored on your device. This cannot be undone." Clicking it should show a confirmation dialog before deleting. Large, readable text throughout.
Overall settings feel: calm, legible, nothing alarming. Every interactive element should be large enough for an elderly user to interact with comfortably without reading glasses.